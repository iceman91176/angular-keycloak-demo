import { SplTranslatePipe } from './spl-translate.pipe';

describe('SplTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new SplTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
