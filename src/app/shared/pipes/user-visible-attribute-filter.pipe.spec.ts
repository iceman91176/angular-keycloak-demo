import { UserVisibleAttributeFilterPipe } from './user-visible-attribute-filter.pipe';

describe('UserVisibleAttributeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserVisibleAttributeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
